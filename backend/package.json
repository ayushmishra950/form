{
  "name": "backend-form",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server.js",
  "scripts": {
    "dev": "nodemon --exec node --import=tsx server.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "typecheck": "tsc --noEmit",
    "dev:memory": "node --import=tsx scripts/dev-memory.ts",
    "make-admin": "node --import=tsx scripts/create-admin.ts",
    "list-users": "node --import=tsx scripts/list-users.ts",
    "build:client": "npm --prefix ../frontend install && npm --prefix ../frontend run build && rm -rf build && cp -r ../frontend/dist build",
    "build:all": "npm run build:client && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "mongodb-memory-server": "^11.2.0",
    "nodemon": "^3.1.14",
    "tsx": "^4.23.13"
  },
  "dependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cookie": "^0.6.0",
    "@types/cookie-parser": "^1.4.10",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^26.5.0",
    "bcryptjs": "^3.0.3",
    "cookie": "^0.7.2",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.9.5",
    "socket.io": "^4.8.3",
    "typescript": "^7.0.2",
    "zod": "^4.5.4"
  }
}
